@model IEnumerable<AspNetCoreProject.Models.MenuModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Menu Permission</h2>

<p>
    <button type="button" class="btn btn-primary"><a asp-action="Create" style="color:white">Create New</a></button>
</p>
<table class="table">
    <thead>
        <tr class="active">
            <th>
               Menu Text
            </th>
            <th>
               Roll Text
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MenuHelperModelIdText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RollIdText)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
